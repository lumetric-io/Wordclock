[env:wordclock-logo]
extends = env:base
board_build.flash_mode = dio
board_build.flash_freq = 40m
board_build.flash_size = 4MB
build_flags =
    ${env:base.build_flags}
    -Iproducts/wordclock-logo
    -include product_config.h
    -DPRODUCT_CONFIG_HEADER=\"product_config.h\"
